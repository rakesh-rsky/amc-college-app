<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amccollege">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutAppActivity"></activity>
        <activity android:name=".AmcCourseActivity" />
        <activity android:name=".StudentCorner" />
        <activity android:name=".SocialMediaActivity" />
        <activity android:name=".FacultyActivity" />
        <activity android:name=".AdmissionActivity" />
        <activity android:name=".FacilitiesActivity" />
        <activity android:name=".AmcecCourseActivity" />
        <activity android:name=".AmcMainActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".WebsiteActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>